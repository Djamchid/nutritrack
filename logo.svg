<svg width="512" height="512" xmlns="http://www.w3.org/2000/svg">
  <!-- Fond avec dégradé -->
  <defs>
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#6366f1;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#4f46e5;stop-opacity:1" />
    </linearGradient>
    <filter id="shadow">
      <feDropShadow dx="0" dy="4" stdDeviation="8" flood-opacity="0.2"/>
    </filter>
  </defs>
  
  <!-- Fond arrondi -->
  <rect width="512" height="512" rx="100" fill="url(#bgGradient)"/>
  
  <!-- Cercle blanc central -->
  <circle cx="256" cy="256" r="180" fill="white" opacity="0.95" filter="url(#shadow)"/>
  
  <!-- Emoji salade -->
  <text x="256" y="310" font-family="Apple Color Emoji, Segoe UI Emoji, sans-serif" font-size="180" text-anchor="middle" fill="#6366f1">🥗</text>
  
  <!-- Texte optionnel -->
  <text x="256" y="420" font-family="Arial, sans-serif" font-size="36" font-weight="bold" text-anchor="middle" fill="white">NutriTrack</text>
</svg>